# System-Resource-Monitor
C++ terminal-based system monitor for Linux. Tracks live CPU, memory, and per-process data without external dependencies by directly parsing the virtual filesystem, utilizing concurrent threads for data fetching and UI rendering.
